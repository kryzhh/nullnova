psutil==6.0.0        # cross-platform device detection
pywin32==306; sys_platform == "win32"   # Windows-only (for raw disk access)
pyudev==0.24.0; sys_platform == "linux" # Linux-only (for USB device info)
